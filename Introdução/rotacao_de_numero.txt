\\ 52) Desenvolva um algoritmo que solicite ao usuário um número 10 dígitos. 
\\ O algoritmo deve rotacionar os dígitos uma posição para a esquerda, de modo que o primeiro dígito passe a ocupar a última posição.
\\ O novo número deve ser mostrado na tela.



algoritmo "rotação p/ esquerda 10 digitos"

var 
dig1,num: inteiro
inicio
escreval("Digite um número com 10 digitos: ")
leia(num)

dig1 <- num\1000000000 // separa o primeiro dígito
num <- num%1000000000 // salva o número sem o primeiro dgito na variável num.

// multiplicar por 10 move o numero uma casa para a esquerda para dar espaço para somar o dígito separado.
escreval("O número rotacionado é: ",num*10+dig1)
fimalgoritmo