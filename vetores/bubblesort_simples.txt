algoritmo "método bubblesort simples"

var
v:vetor [0..9] de inteiro
i,j,aux: inteiro
inicio
escreval ("Informe os valores do vetor: ")
para i de 0 ate 9 faca
leia (v[i])
fimpara

escreval("Vetor digitado: ")
para i de 0 ate 9 faca
escreva(v[i], " ")
fimpara
escreval()
para i de 0 ate 9 faca
         para j de 0 ate 8 faca
                  se (v[j] > v[j+1]) entao
                           aux<- v[j]
                           v[j] <- v[j+1]
                           v[j+1] <- aux
                  fimse
         fimpara
         escreval(i, "ª iteração")
         para j de 0 ate 9 faca
                  escreva (v[j], " ")
         fimpara
         escreval()
fimpara

fimalgoritmo
